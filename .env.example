# Variables de entorno ejemplo
APP_ENV=development
APP_DEBUG=true

# Dominio / URL base de la aplicación (cambiar cuando el dominio esté autorizado)
APP_DOMAIN=new-domain.example
APP_PROTOCOL=http
BASE_URL=${APP_PROTOCOL}://${APP_DOMAIN}
